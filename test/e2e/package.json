{
  "name": "jellyfin-helper-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end test harness for the Jellyfin Helper plugin (real Jellyfin 12 container + mock Arr/Seerr).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
