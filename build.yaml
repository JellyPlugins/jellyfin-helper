---
name: "Jellyfin Helper"
guid: "0c737645-5cbb-4bd8-80c7-d377b560aaa4"
version: "2.0.4"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Automated cleanup (trickplay, empty folders, subtitles), media statistics with export/trends, trash bin, Arr integration, and multi-language dashboard."
description: >
  A Jellyfin plugin that automatically cleans up orphaned .trickplay folders, media folders that no longer contain any video files, and provides media library statistics.

category: "General"
owner: "JellyPlugins"
artifacts:
  - "Jellyfin.Plugin.JellyfinHelper.dll"
changelog: >-
  Critical bugfix: Music libraries and Boxset/Collection folders are now fully protected from deletion. Audio files (.mp3, .flac, .wav, etc.) are recognized as valid media. Folders containing [boxset] or [collection] tags are always skipped. Collections path locations are filtered at library level. Fixed bug where safeLocations filter was computed but not returned. 196 tests.
