---
name: "Jellyfin Helper"
guid: "0c737645-5cbb-4bd8-80c7-d377b560aaa4"
version: "3.0.0.4"
targetAbi: ""
framework: "net10.0"
overview: "Automated cleanup (trickplay, empty folders, subtitles, link repair for .strm & symlinks), Seerr request cleanup, media statistics with codec/health/dynamic-range analysis and Library Explorer search, ML-powered smart recommendations per user, user activity insights, library insights, backup/restore, trends, trash bin, plugin log viewer, multi-instance Arr integration with collapsible UI, unsaved settings alert, and multi-language 8-tab dashboard."
description: >
  A Jellyfin plugin providing automated cleanup tasks (trickplay, empty folders, subtitles, link repair for .strm files and symlinks), media library statistics with codec/resolution/bitrate/language/dynamic-range/health analysis and Library Explorer search via MediaStream metadata, ML-powered smart recommendations with ensemble scoring strategy (heuristic + learned + neural MLP blend) and per-user watch profiles, user activity insights with play count and completion tracking, library insights (largest directories & recent changes), historical trends, trash/recycle bin, multi-instance Arr stack integration (Radarr/Sonarr), plugin-specific log viewer with download and filtering, backup/restore data, and a multi-language 8-tab dashboard.

category: "General"
owner: "JellyPlugins"
artifacts:
  - "Jellyfin.Plugin.JellyfinHelper.dll"
changelog: |-
  v3.0.0.4 - Per-library file counts, boxset link fix

  - The Per-Library Breakdown now shows a file count below every size value (trickplay folders included), so sparse and dense libraries are distinguishable at a glance
  - Boxset library rows no longer link into the Library Explorer, whose scope excludes boxsets; they render as plain text instead of opening an empty state
  - Every task dropdown in Settings now explains what the task does, on hover, in all dashboard languages
  - The Overview Trickplay card now also shows the space used by trickplay images Jellyfin stores internally, on its own line below the alongside-media total, marked with an (internal) badge
  - Compact log view on phones: log entries stack as cards below 700 px instead of squeezing into a four-column table, so no horizontal scrolling is needed
  - Roomier Library Explorer filter popovers on phones using nearly the full viewport width, like the bitrate editor
  - Tested by 6196 unit tests and 349 end-to-end tests
  - Requires Jellyfin 12.1 - if you are on Jellyfin 10.x, stay on v2.1.0.6
